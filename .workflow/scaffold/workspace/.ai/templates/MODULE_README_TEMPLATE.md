# 模块说明文档 (Module README)

> **模块名称**: [输入模块的英文或中文名称]
> **维护者**: [输入负责的开发者或团队]

## 1. 核心职责 (Responsibility)
[用一到两句话，简明扼要地说明本模块的核心业务边界和主要功能。解答“它为什么存在”这个问题。]

## 2. 外部契约 (Contracts & API)
[本模块对外部系统、其他同级模块或前端暴露的核心接口、事件与数据结构。]
- **核心接口 1**: `GET /api/v1/module/action` - [简述功能]
- **核心接口 2**: `POST /api/v1/module/submit` - [简述功能]
- **对外事件**: `ModuleStatusUpdatedEvent` - [何时触发]

## 3. 核心依赖 (Dependencies)
[列出本模块强依赖的外部微服务、内部其他模块或关键的第三方中间件。]
- [依赖项 1，例如：用户权限模块（需验证 Token）]
- [依赖项 2，例如：Redis 缓存（用于防重放机制）]

## 4. 目录结构说明 (Structure)
简述模块内部的代码组织方式，帮助新接手的开发者快速建立心智模型。

```text
.
├── api/          # 对外暴露的接口定义与路由注册
├── core/         # 核心业务逻辑实现 (Services)
├── models/       # 本模块的领域数据模型与 DTO
└── tests/        # 模块专属的单元与集成测试
```